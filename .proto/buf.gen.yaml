version: v1
managed:
  enabled: true
  go_package_prefix:
    default: github.com/bufbuild/buf-tour/gen
breaking:
  use:
    - FILE    
plugins:
  - plugin: go
    out: gen
    opt: paths=source_relative

